[pytest]
python_files = tests.py test_*.py *_tests.py
python_classes = *Tests Test*
python_functions = test_*
testpaths = .
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: integration tests touching the DB
